{
  "server" : "0.0.0.0",
{% if shadowsocks_enable_multiport %}
  "port_password" : {{ port_password | safe}},
{% else %}
  "server_port" : {{ port }},
  "password" : "{{ password }}",
{% endif %}
  "method": "{{ method }}",
  "timeout": 10
}
